[alias]
  st = status
  di = diff
  co = checkout
  ci = "commit -v"
  rv = "revert"
  br = branch
  llog = "log --date=local"
  flog = "log --pretty=fuller --decorate"
  lg = "log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative"
  lol = "log --graph --decorate --oneline"
  lola = "log --graph --decorate --oneline --all"
  blog = "log origin/master... --left-right"
  ds = "diff --staged"
  sw = switch
  fixup = "commit --fixup"
  squash = "commit --squash"
  unstage = "reset HEAD"
[core]
  editor = nvim
  excludesfile = ~/.gitignore_global
  hooksPath = /Users/mgary/workspace/git-hooks-core
[url "git@github.com:"]
  insteadOf = https://github.com/
[http "https://gopkg.in"]
  followRedirects = true
[merge]
  conflictstyle = diff3
[push]
  default = simple
[submodule]
  fetchJobs = 6
[diff]
  compactionHeuristic = true
[commit]
  verbose = true
	gpgsign = true
[user]
  name = Mirah Gary
  email = mgary@vmware.com
[gpg]
  program = /usr/local/bin/gpg
[credential]
  helper = osxkeychain
[pull]
	rebase = false
[init]
	defaultBranch = main
